# ============================================================================
# Secrete: .env (per platforma) e gitignored si NU se comite NICIODATA.
# Config NEsensibil:   config.env (per platforma)  -> SE comite (versionat).
# Template secrete:    .env.example (per platforma) -> SE comite (placeholders).
# ============================================================================
.env
.claude/settings.local.json

# Editor swap / temporar
*.swp
*.swo
*.tmp
*~

# Cache files (runtime, per-proces) — acum in subfolderul cachedb/
cachedb/
cache_*.json
last_watch_config.json
priceanalysis.json
index.html

# Build
*.egg-info/
__pycache__/
*.pyc
.pytest_cache/
.venv/
.myenv/
*.log
*.log.[0-9]*
.logrotate.state
*.pem
cache_*.jsonl
*.meta
*.bak
*.json
*.lock
trade_cooldown.json
