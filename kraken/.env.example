# ============================================================================
# Kraken — SECRETE. Copiaza in .env si completeaza:  cp .env.example .env
# Config-ul (pereche, parametri strategie) e in config.env (deja comis).
# Permisiuni cheie API: Query Funds, Query Open/Closed Orders, Create & Modify
# Orders. NU da permisiuni de withdrawal.
# ============================================================================
KRAKEN_API_KEY=
KRAKEN_API_SECRET=

# Notificari
NTFY_TOPIC=
ALERT_TO_EMAIL=
SMTP_USERNAME=
SMTP_PASSWORD=
