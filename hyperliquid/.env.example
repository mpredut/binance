# ============================================================================
# Hyperliquid — SECRETE. Copiaza in .env si completeaza:  cp .env.example .env
# Config-ul (moneda, strategie, delta-neutral) e in config.env (deja comis).
#
# AUTENTIFICARE: foloseste un AGENT / API WALLET, NU cheia contului principal!
#   More -> API -> "Generate" agent wallet -> Approve.
#   Agentul poate tranzactiona, dar NU poate retrage fonduri.
# ============================================================================
HL_SECRET_KEY=""        # cheia privata a agent wallet-ului (0x...)
HL_ACCOUNT_ADDRESS=""   # adresa contului principal (cel cu USDC)

# Notificari
NTFY_TOPIC=
ALERT_TO_EMAIL=
SMTP_USERNAME=
SMTP_PASSWORD=
